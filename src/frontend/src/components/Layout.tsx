/**
 * Legacy layout shim.
 *
 * The canonical layout lives at `@/components/layout/AppLayout`. This module
 * is retained only for backwards-compatible import paths; new code should
 * import `AppLayout` from `@/components/layout/AppLayout` directly.
 */

export { AppLayout, default } from "@/components/layout/AppLayout";
